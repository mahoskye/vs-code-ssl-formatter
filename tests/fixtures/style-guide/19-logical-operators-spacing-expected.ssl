/* Logical operators should have proper spacing;
:PROCEDURE TestLogicalOperators;
:PARAMETERS bCondition;

	:IF bCondition .AND. nValue > 0;
		nResult := 1;
	:ELSE;
		nResult := 0. OR .2;
	:ENDIF;
:ENDPROC;
