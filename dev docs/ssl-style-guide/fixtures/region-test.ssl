:PROCEDURE TestRegions;

/* region Database Setup;
:VAR sConnection := "Server=localhost;Database=test";
:VAR bConnected := .F.;
/* endregion;

/*region Validation;
:IF sConnection = "";
    usrmes("Connection string is empty", "Error");
    :RETURN .F.;
:ENDIF;
/*endregion;

/* region Main Processing;
    /* region Inner Section;
    :VAR i := 0;
    :FOR i := 1 :TO 10;
        usrmes(Str(i));
    :NEXT;
    /* endregion;
    
    :VAR sResult := "Complete";
/* endregion;

:RETURN .T.;
:ENDPROC;
